language: julia

os:
  - linux

julia:
  - 1.0

after_success:
  - julia -e 'using Pkg; Pkg.add("Coverage"); import Hamiltonian; joinpath(dirname(pathof(Hamiltonian))); using Coverage; Codecov.submit(Codecov.process_folder())'
  - julia -e 'using Pkg; Pkg.add("Documenter"); import Hamiltonian; joinpath(dirname(pathof(Hamiltonian))); include(joinpath("docs","make.jl"))'
